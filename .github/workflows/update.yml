name: Update V2ray Configs

on:
  schedule:
    - cron: '0 */1 * * *' # این یعنی هر 1 ساعت یکبار چک میکند و آپدیت میکند
  workflow_dispatch: # این دکمه دستی برای آپدیت فوری است

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write # اجازه نوشتن در فایل را میدهد
      
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install requests beautifulsoup4

      - name: Run Scraper
        run: python scraper.py

      - name: Commit and Push changes
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git add yasin.txt
          git commit -m "🚀 Auto-update configs from Telegram" || exit 0
          git push
